/**
 * This file is intentionally left as a no-op stub.
 * The Google Translate runtime path was removed because all locale message
 * files are pre-built and live in messages/<locale>.json.
 * The /api/translate route still exists for manual regeneration if needed.
 */
export {};
